import { CorrectedValidatedField } from './corrected-validated-field';

describe('MutatedValidatedField', () => {
  it('should create an instance', () => {
    expect(new CorrectedValidatedField()).toBeTruthy();
  });
});
