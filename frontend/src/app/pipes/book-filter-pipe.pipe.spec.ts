import { BookFilterPipePipe } from './book-filter-pipe.pipe';

describe('BookFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BookFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
