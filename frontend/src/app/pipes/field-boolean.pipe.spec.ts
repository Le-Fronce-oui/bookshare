import { FieldBooleanPipe } from './field-boolean.pipe';

describe('FieldBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
