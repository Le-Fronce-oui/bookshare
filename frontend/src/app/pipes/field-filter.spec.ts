import { FieldFilterPipe } from './field-filter.pipe';

describe('FieldFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FieldFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
